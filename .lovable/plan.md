

## Plan: Add "Sorix Deck" to Pricing + 20 Free Slides for Free Users

### Summary
1. Add "Sorix Deck" as a feature in all pricing plans across all 3 locations (landing page, upgrade modal, settings tab)
2. Free plan: "Sorix Deck" with subtext "20 slides free" 
3. Paid plans: "Sorix Deck" included (unlimited, uses tokens)
4. Backend: Allow free users to generate up to 20 slides without token deduction, track usage in a counter
5. Position "Sorix Deck" below tokens row in feature lists (like image shows — tools section below tokens)

### Files to Change

**1. `src/components/Pricing.jsx`** — Add `Sorix Deck` feature to all 4 plans
- Free plan: `{ text: 'Sorix Deck', subtext: '20 slides free', included: true }`
- Basic/Pro/Premium: `{ text: 'Sorix Deck', included: true }` — insert after Image Gen row

**2. `src/components/aichat/UpgradePlanModal.tsx`** — Same additions to all 4 plan feature arrays
- Free: `{ text: 'Sorix Deck', subtext: '20 slides free', included: true }`
- Others: `{ text: 'Sorix Deck', included: true }`

**3. `src/components/aichat/settings/PlansTokensTab.tsx`** — Add Sorix Deck tool card in the "Free Tools" section with Presentation icon, showing "20 Free Slides" badge for free users, "Included" for paid

**4. `supabase/functions/deck-generate/index.ts`** — Backend logic changes:
- Query `presentations` table to count total slides generated by user (`SUM(slide_count)`)
- If user is on free plan AND total slides generated so far < 20: allow generation without token deduction (skip token check, skip token update)
- If free plan user has used 20+ slides: return `insufficient_tokens` error to trigger upgrade modal
- Paid plan users: no change, tokens deducted as usual

**5. `src/pages/DeckPage.tsx`** — Update the token info display:
- For free users: show "X/20 free slides used" instead of token info
- Adjust the `estimatedCost` logic: if free plan and under limit, show "Free" instead of token cost

### Database
- No schema changes needed — `presentations.slide_count` already tracks slides per presentation, can be summed

